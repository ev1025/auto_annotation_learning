# 설계 변경 이력 (CHANGELOG)

README 본문은 항상 "현재 구조"만 기술한다. 무엇이 어떻게 바뀌었는지는 이 파일에 계속 기록한다.
형식: 날짜 / 기존 → 변경 / 사유 / 파생 영향.

<br>

## 2026-07-22 학습 데이터 아키텍처 피벗 (가장 큰 변경)

**기존**: 3D 렌더 합성 데이터가 학습 데이터의 원천. 외부(3D팀)가 "2D 사진 → 3D 모델 →
다시점 렌더 + 3D 좌표 투영 어노테이션"으로 (이미지+정답박스)를 공급하면 파이프라인이
반입(`0_import_render.py`)해 학습. 렌더→실사 도메인 갭이 핵심 리스크였고, 이를 위해
렌더 공급 조건(배경·조명·가림 랜덤화, 수량 기준)과 실사 파인튜닝(`7_finetune_real.py`)을 준비.

**변경**: 사용자가 업로드한 부품 2D 사진(영상 프레임 포함)이 학습 데이터의 원천.
`0_register_part.py`가 상호 일관성 매칭(SAM+DINOv2)으로 라벨을 자동 생성해 반입.
2D→3D 변환은 병렬로 수행하되 **XR 화면 표시용으로만 쓰고 학습 경로에서 제외**.

**사유**: 2D→3D 변환이 학습 어노테이션에 쓸 만큼 정교하지 않을 것으로 판단.

**파생 영향**:
- 도메인 갭 문제 소멸 (학습 데이터가 처음부터 실사)
- 새 병목 = 수량 (부품당 사진 수십 장 수준. 영상 프레임 추출·SAM2 전파로 보완 예정)
- 구 논의사항 1(클래스 정의 A/B/C안) 해소: 업로드 폴더명 = 부품명으로 확정
- 구 논의사항 2(렌더 공급 조건: 도메인 랜덤화 표) 폐기
- 참고자료 [1][3][4][6]의 합성 학습 부분 참고 가치 하향
- 운영 흐름: 업로드 목록에 부품 여러 개를 쌓고 배치 처리, 재학습은 배치당 1회
  (시재품 = 기본 장비를 같은 절차로 사전 등록해 출고 / 완성품 = 신규 장비 등록 시 사용자가 실행)

**등록 라벨링 방식의 선정 근거** (zeroshot_labeler/eval_out, 정답 대조 실측):
| 방식 | 정밀도 | 비고 |
|------|--------|------|
| 텍스트 프롬프트 제로샷 (Grounding DINO) | 0.24 | 유사 부품 클래스 혼동으로 탈락 |
| Grounded-SAM (텍스트+타이트박스) | 0.24 | 박스 여백은 문제가 아니었음(TP IoU 0.91) |
| SAM + CLIP 갤러리 | 0.60 | 시각 매칭 우월성 확인 |
| SAM + DINOv2 갤러리 (τ0.85) | 0.93 | 고순도 운영점 달성 |
| **채택: 상호 일관성 매칭** (SAM+DINOv2) | 시뮬 채택률 100% | 업로드=부품 1종 전제로 클래스 분류 자체를 제거 |

## 2026-07-22 배포 모델 확정: YOLOv8n → YOLO26s @ 640

**기존**: YOLOv8n(경량 기본값). **변경**: YOLO26s, 입력 640 확정.
**사유**: 7모델×2크기 벤치마크 + 시드 3반복 실측 (README 5.5). 정확도 동급(mAP50-95 평균 1위),
시드 분산 1/6, 지연 최단(7.0ms, NMS-free), Thor TensorRT 적합.
**파생**: 5.2~5.4의 기존 실측치는 v8n 기준(루프 유효성 결론은 불변).

## 2026-07-22 학습 기록 체계 보강

- 학습률(산입률) 콜백: "입력 N장 중 M장 산입 = X%" + 제외 파일명 로그 (시험항목 6 증빙)
- metrics.json 에 trained_at(학습 일시) 명시, models/releases/history.jsonl 누적 이력 신설

## 2026-07-21 지속 배포 안전장치 도입

**기존**: 학습 결과가 서빙 모델(new_model.pt)을 무조건 덮어씀, 이전 버전·학습 로그 미보존.
**변경**: 릴리스 버전 보관(best/onnx/지표/학습로그) + 배포 게이트(직전 채택본 대비 mAP50
하락 시 미교체) + 원터치 롤백(`6_model_registry.py`).
**사유**: 오토러닝 지속 배포에서 나쁜 모델의 자동 배포 차단과 사고 시 복원 수단 필요.

## 2026-07-21 배포 구조 확정: Jetson Thor 원격 추론

**기존**: 배포 형태 미정(FastAPI 서버만 존재). **변경**: 클라이언트(Magic Leap 2·태블릿)는
프레임 전송/JSON 수신만, Thor 서버가 전처리·추론·후처리 전담.
**사유**: 기기별 온디바이스 구현 부담 제거, 기종 의존성 제거.

## 2026-07-16 오토러닝 유효성 실증 완료 (초기 구축)

- 파이프라인 구축(자동라벨→학습→서빙) + self-training 효과 실증: 4개 조건 전부
  라운드1 > 라운드0 (+1.0~+3.6%p), 자동 라벨 정밀도 0.87~0.90 (README 5.3)
- 증분 학습 수정: 신규 데이터 유입 시 val 고정 + train 만 추가 (평가 오염 방지)
