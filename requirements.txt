# 핵심 의존성
ultralytics>=8.3.0        # YOLO 학습/추론/ONNX export
fastapi>=0.110            # 대시보드·추론 API 웹 프레임워크
uvicorn[standard]>=0.29   # ASGI 서버
python-multipart>=0.0.9   # FastAPI 파일 업로드(multipart/form-data) 처리
pillow>=10.0              # 이미지 디코딩
numpy>=1.26
opencv-python>=4.9        # 프레임 추출·합성 증강·결과 시각화

# SAM2 오토라벨 (참조 프레임 마스크 -> 영상 전 프레임 전파)
# pip 이름은 SAM-2. 설치: pip install "sam2 @ git+https://github.com/facebookresearch/sam2.git"
# 체크포인트(sam2.1_hiera_base_plus.pt, 309MB)는 models/sam2/ 에 별도 배치(git 미추적)
sam2
hydra-core>=1.3           # sam2 설정 로더
iopath>=0.1.10            # sam2 체크포인트 IO

# 메타데이터 DB (부품·영상·프레임·주석·학습이력 색인)
SQLAlchemy>=2.0
psycopg[binary]>=3.1      # PostgreSQL 드라이버(psycopg3)

# ONNX 변환 및 ONNX 서빙용
onnx>=1.16
onnxruntime>=1.17         # GPU 서빙은 onnxruntime-gpu 로 교체
onnxslim>=0.1.34          # export(simplify=True) 시 그래프 단순화

# 실험 스크립트 전용(벤치마크·데이터 반입에서 data.yaml 파싱)
PyYAML>=6.0

# 참고: torch 는 ultralytics 가 끌어오지만, GPU(CUDA) 사용 시에는
#       https://pytorch.org 안내대로 CUDA 버전에 맞는 torch 를 먼저 설치한다.
#       예) pip install torch --index-url https://download.pytorch.org/whl/cu124
#       Jetson(Thor)은 pip 기본 휠이 없다. L4T 베이스 이미지에 들어 있는 torch 를 그대로 쓴다.
